/**
 * 백엔드 PR #34(`feature/Bae/team-chat`, `/chat` 네임스페이스 ChatGateway)는 아직
 * develop에 merge되지 않았지만, 이 브랜치(feature/Ryu/team-chat)에는 이미 그 백엔드
 * 코드가 merge되어 있어 로컬에서 이 브랜치로 백엔드를 띄우면 실제로 연결된다.
 *
 * PR #34가 develop에 merge되면 이 주석만 정리하면 된다.
 */
export const CHAT_ENABLED = true;
